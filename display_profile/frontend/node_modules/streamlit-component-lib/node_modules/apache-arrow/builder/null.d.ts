import { Null } from 'streamlit-component-lib/node_modules/apache-arrow/type';
import { Builder } from 'streamlit-component-lib/node_modules/apache-arrow/builder';
/** @ignore */
export declare class NullBuilder<TNull = any> extends Builder<Null, TNull> {
    setValue(index: number, value: null): void;
    setValid(index: number, valid: boolean): boolean;
}
