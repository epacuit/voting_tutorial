import { Bool } from 'streamlit-component-lib/node_modules/apache-arrow/type';
import { Builder, BuilderOptions } from 'streamlit-component-lib/node_modules/apache-arrow/builder';
/** @ignore */
export declare class BoolBuilder<TNull = any> extends Builder<Bool, TNull> {
    constructor(options: BuilderOptions<Bool, TNull>);
    setValue(index: number, value: boolean): void;
}
