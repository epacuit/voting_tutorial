import { Builder } from 'streamlit-component-lib/node_modules/apache-arrow/builder';
import { DataType, Struct } from 'streamlit-component-lib/node_modules/apache-arrow/type';
/** @ignore */
export declare class StructBuilder<T extends {
    [key: string]: DataType;
} = any, TNull = any> extends Builder<Struct<T>, TNull> {
    addChild(child: Builder, name?: string): number;
}
