import { BaseVector } from './base';
import { DataType, Map_, Struct } from 'streamlit-component-lib/node_modules/apache-arrow/type';
/** @ignore */
export declare class MapVector<K extends DataType = any, V extends DataType = any> extends BaseVector<Map_<K, V>> {
    asList(): import("streamlit-component-lib/node_modules/apache-arrow/vector/list").ListVector<Struct<{
        key: K;
        value: V;
    }>>;
    bind(index: number): Map_<K, V>['TValue'];
}
